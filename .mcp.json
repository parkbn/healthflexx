{
  "mcpServers": {
    "google-calendar": {
      "command": "node",
      "args": ["mcp-servers/google-calendar/build/index.js"],
      "env": {
        "GOOGLE_CALENDAR_ICAL_URL": "${GOOGLE_CALENDAR_ICAL_URL}"
      }
    }
  }
}
